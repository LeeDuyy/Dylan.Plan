# language.md — Chuẩn ngôn ngữ của dự án

Updated: <YYYY-MM-DD>
Scope: Dự án `<tên dự án>`.
Kế thừa: `${CLAUDE_PLUGIN_ROOT}/memory/language.md` (kit-level).

Chỉ ghi phần KHÁC hoặc BỔ SUNG so với kit-level.

---

## 1. Ngôn ngữ đầu ra

- Ngôn ngữ artifact: <vi | en>
- Ngoại lệ giữ nguyên tiếng Anh: <danh sách bổ sung ngoài kit-level>

## 2. Thuật ngữ ưu tiên

Khi cùng một khái niệm có nhiều cách gọi, dùng cột "Dùng".

| Dùng | Không dùng | Lý do |
| --- | --- | --- |
| <thuật ngữ chuẩn> | <biến thể bị cấm> | <lý do> |

## 3. Nhãn trạng thái riêng của dự án

| Nhóm | Giá trị hợp lệ | Nơi xuất hiện |
| --- | --- | --- |
| <nhóm> | `<giá trị>` | `<màn hình / API / báo cáo>` |

## 4. Quy ước đặt tên riêng

| Đối tượng | Quy ước | Ví dụ |
| --- | --- | --- |
| <đối tượng> | <quy ước> | <ví dụ> |

## 5. Nhãn hiển thị cho người dùng cuối

| Khóa kỹ thuật | Nhãn hiển thị | Màn hình |
| --- | --- | --- |
| `<key>` | <nhãn tiếng Việt> | `<màn hình>` |

Nhãn hiển thị sai lệch giữa các màn hình là finding, không phải chuyện nhỏ.
