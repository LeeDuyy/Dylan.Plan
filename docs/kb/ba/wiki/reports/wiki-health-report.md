---
status: Active
updated: 2026-08-21
owner: ssr-ingest
tags: [kb/ba/wiki/reports]
---


# Wiki Health Report

> Cập nhật bởi `ssr-ingest` cuối mỗi lần chạy (`ingest` hoặc `sync`). P0: chỉ đếm số lượng cơ bản — chưa làm lint đầy đủ (orphan pages, broken links).

## 1. Tổng Quan Số Lượng

| Loại trang | Số lượng |
| --- | --- |
| Feature (nested) | 17 (16 `Active` gồm `US-007`, `US-008`, `US-018`, `US-019`, `US-020`; `US-009` mới `Draft`, spec chưa viết; `US-013` gộp vào `US-006`, không tính riêng) |
| Epic | 4 (US-018, US-020 không gắn epic nào — xem mục 2; US-019 chính thức gắn `EPC-003`, `DEC-105`; `EPC-004` (F4) có 2 function: `US-007`, `US-008` — cả hai `Active`; `EPC-002` (F2) nay có thêm `US-009` `Draft`) |
| Concept | 0 |
| Business rule | 30 (mới: `BR-030`) |
| Workflow | 0 |
| Entity | 6 (`ENT-004-job-ung-tuyen` mở rộng cho `US-020`: thêm mốc "Ngày nộp hồ sơ", trạng thái "Expired") |
| PBI | 15 đã đồng bộ (US-002 5 AC, US-003 3 AC, US-004 11 AC, US-005 6 AC, US-006 7 AC — gồm 2 AC gộp từ US-013, US-007 4 AC, US-010 7 AC, US-012 5 AC, US-014 5 AC, US-015 6 AC, US-016 8 AC, US-017 8 AC, US-018 11 AC, US-019 10 AC, US-020 9 AC) |

## 2. Trang Thiếu Nguồn

| Trang | Thiếu gì |
| --- | --- |
| `ENT-004-job-ung-tuyen`, `ENT-005-platform-tuyen-dung` | Chưa có model Prisma — entity mới/mở rộng, chờ `ssr-data` khi `ssr-plan` của `US-018`/`US-020` tới lượt |
| `ENT-006-item-can-mua` | Chưa có model Prisma — entity mới, chờ `ssr-data` khi `ssr-plan` của `US-019` tới lượt |

## 3. Conflict Chưa Xử Lý

Không có.

## 4. Ingest Gần Đây

| Ngày | Function | Mode | Trang bị tạo/sửa |
| --- | --- | --- | --- |
| 2026-08-05 | `US-004` | ingest | 10 (feature, feature-summary, pbi rỗng, source-record, epic, entity, 5 business-rule) |
| 2026-08-05 | `US-004` | sync | 5 (feature → Active, pbi → 11 AC, feature-summary, source-record, indexes) |
| 2026-08-05 | `US-002` | ingest | 5 (feature, feature-summary, pbi rỗng, source-record, 1 business-rule mới `BR-006`) — không tạo epic mới (cross-cutting F1-F4), gọi bởi `ssr-ba` trong `ssr-pipeline US-002` |
| 2026-08-05 | `US-002` | sync | 4 (feature → Active, pbi → 5 AC, feature-summary, source-record) — spec đạt `Ready for DEV` sau 2 lượt dialog (`DEC-049`..`DEC-052`) |
| 2026-08-05 | `US-003` | ingest | 5 (feature, feature-summary, pbi rỗng, source-record, 1 business-rule mới `BR-007`) — gắn vào `EPC-001` (F1); requirement đã triển khai thật cùng đợt US-001, chu trình này chỉ tổng hợp lại artifact, gọi bởi `ssr-ba` trong `ssr-pipeline US-003` |
| 2026-08-05 | `US-003` | sync | 4 (feature → Active, pbi → 3 AC, feature-summary, source-record) — spec đạt `Ready for DEV` ngay lượt đầu (không cần dialog, không phát sinh `Cần user xác nhận`) |
| 2026-08-06 | `US-005` | ingest | 9 (feature, feature-summary, pbi rỗng, source-record, epic mới `EPC-002` cho F2, entity mới `ENT-002-danh-muc`, 5 business-rule mới `BR-008`..`BR-012`) — lần đầu F2 có epic trong cấu trúc nested, gọi bởi `ssr-ba` trong `ssr-pipeline US-005` |
| 2026-08-06 | `US-005` | sync | 4 (feature → Active, pbi → 6 AC, feature-summary, source-record) — spec đạt `Ready for DEV` sau 1 lượt dialog 3 câu (`DEC-054`..`DEC-057`) |

| 2026-08-06 | `US-012` | ingest | 5 (feature `Draft`, feature-summary, pbi rỗng, source-record, 1 business-rule mới `BR-013`) — không tạo epic mới, gắn vào `EPC-001` (F1) đã có sẵn; requirement là defect fix phát hiện qua PO review (`docs/po/review-2026-08-06-nhap-nhanh-nhan-dien-danh-muc.md`), gọi bởi `ssr-ba` trong `ssr-pipeline US-012` |
| 2026-08-06 | `US-012` | sync | 5 (feature → Active, pbi → 5 AC, feature-summary, source-record, `BR-011` thêm liên kết) — spec đạt `Ready for DEV` sau 2 lượt dialog (`DEC-059` ở `ssr-po`, `DEC-060` ở `ssr-ba`), `po-expert` Aligned, `ba-expert` bổ sung AC-05 |
| 2026-08-06 | `US-006` | ingest | 7 (feature `Draft`, feature-summary, pbi rỗng, source-record, epic mới `EPC-003` cho F3, entity mới `ENT-003-thang-ngan-sach`, business rule mới `BR-014`) — lần đầu F3 có epic trong cấu trúc nested; migrate từ trang phẳng cũ, gọi bởi `ssr-ba` trong `ssr-pipeline US-006`; còn 1 điểm `Cần user xác nhận` (Q1 gốc từ raw) gắn nhãn ở `BR-014` |
| 2026-08-07 | `US-006` | sync | 5 (feature → Active, pbi → 5 AC, feature-summary, source-record, `BR-014` đổi nội dung cho khớp hướng đã chốt) — spec đạt `Ready for DEV` sau 2 lượt dialog (`DEC-061` chốt ngăn trùng bằng disable trong combobox thay vì báo lỗi sau khi bấm, `DEC-062` chốt khoảng 6 tháng trước/sau), `po-expert` Aligned, `ba-expert` sửa 3 điểm nhất quán nội bộ (không đổi ý định nghiệp vụ) |
| 2026-08-10 | `US-006` | sync (gộp US-013) | 6 (feature cập nhật, pbi → 7 AC, feature-summary, source-record, `BR-014` đổi tên nút trong mô tả, `BR-015` mint mới) — raw `US-013` (đổi nhãn "Chọn tháng" → "Chọn tháng xem", tách khu vực tạo tháng mới, đổi tên/nghiệp vụ nút "Clone tháng hiện tại" → "Clone tháng đang xem") gộp thẳng vào spec `US-006` thay vì tách spec riêng, theo yêu cầu đánh giá impact của user (`DEC-065`); 2 quyết định nghiệp vụ con `DEC-063`, `DEC-064` chốt qua dialog khi ghi raw `US-013`; `spec-quality.mjs --strict` 0 lỗi |
| 2026-08-10 | `US-014` | ingest | 6 (feature `Draft`, feature-summary, pbi rỗng, source-record, business rule mới `BR-016`) — gắn vào `EPC-002` (F2) đã có sẵn, liên kết thêm `ENT-002-danh-muc`, không tạo epic/entity mới; gọi bởi `ssr-ba` trong `ssr-pipeline US-014`; `openItemsCount=0` (raw đã tự trả lời cả 2 câu hỏi mở) |
| 2026-08-10 | `US-014` | sync | 5 (feature → Active, pbi → 5 AC, feature-summary, source-record, `BR-016` thêm liên kết `EL-##`) — spec đạt `Ready for DEV` sau 1 lượt dialog xác nhận phạm vi 3 nơi áp dụng (`DEC-066`); `po-expert` ban đầu `Needs Adjustment` (câu hỏi thứ tự ưu tiên với `US-010`, chưa có spec) → user quyết định làm `US-014` trước qua dialog (`DEC-067`) → `po-expert` xác nhận lại `Aligned`; `ba-expert` sửa 2 điểm (mặc định dropdown `EL-02`, ghi chú gán tạm `Loại: Table` cho `EL-03` vì kit chưa có Loại "Chart") |
| 2026-08-10 | `US-010` | ingest | 6 (feature `Draft`, feature-summary, pbi rỗng, source-record, business rule mới `BR-017`) — gắn vào `EPC-002` (F2) đã có sẵn, liên kết thêm `ENT-002-danh-muc`, không tạo epic/entity mới; migrate từ trang phẳng cũ; gọi bởi `ssr-ba` trong `ssr-pipeline US-010`; `openItemsCount=0` (raw đã tự trả lời đủ qua `DEC-020`, `DEC-021`, `DEC-022`, `DEC-027`) |
| 2026-08-10 | `US-010` | sync | 5 (feature → Active, pbi → 7 AC, feature-summary, source-record, `BR-017` bổ sung liên kết `DEC-068`/`DEC-069`) — spec đạt `Ready for DEV` sau 2 lượt dialog: (1) `DEC-068` chốt áp ràng buộc trùng tên luôn cho tên mặc định "Danh mục mới" của nút "Thêm danh mục"; (2) `DEC-069` chốt mở rộng chuẩn hóa rút gọn khoảng trắng lặp ở giữa chuỗi, theo đề xuất phát hiện được của `ba-expert`; `po-expert` xác nhận `Aligned` (đúng M1, F2/F1, không mâu thuẫn DEC nào Active); `ba-expert` sửa 4 điểm (ngoại lệ "không có dữ liệu" mâu thuẫn nội bộ, AC-03 kiểm chứng hành vi UI không tồn tại, thiếu AC cho ngoại lệ danh mục duy nhất, thiếu liên kết phụ thuộc `US-005`); `spec-quality.mjs --strict` 0 lỗi |
| 2026-08-11 | `US-015` | ingest | 6 (feature `Draft`, feature-summary, pbi rỗng, source-record, business rule mới `BR-018`) — không tạo epic mới, gắn vào `EPC-003` (F3) đã có sẵn, liên kết thêm `ENT-003-thang-ngan-sach`; requirement là opportunity UI/UX phát hiện qua PO review (`docs/po/review-2026-08-11-quick-view-thang.md`, finding PO-02), gọi bởi `ssr-ba` trong `ssr-pipeline US-015`; `openItemsCount=0` (2 câu hỏi mở của raw đã tự trả lời từ `DEC-071`, `DEC-072`; câu hỏi Q3 về hành vi click-để-chọn là giả định hợp lý, không chặn) |
| 2026-08-11 | `US-015` | sync | 5 (feature → Active, pbi → 6 AC, feature-summary, source-record, `EPC-003` mục 4 cập nhật trạng thái) — spec đạt `Ready for DEV` ngay lượt đầu, không cần dialog (0 câu hỏi `Cần user xác nhận` — 2 điểm mờ của raw đã chốt trước thành `DEC-071`/`DEC-072` qua `ssr-po mode=review`); `po-expert` xác nhận `Aligned` (chỉ lưu ý nhỏ mục 2 spec ghi nhầm mục tiêu M1 thay vì M2 của luồng F3, đã sửa); `ba-expert` sửa 2 điểm không đổi ý định nghiệp vụ (xóa dòng "Ngoài phạm vi:" thừa, chuẩn hóa nhãn trạng thái phụ thuộc US-001/US-006 thành "Delivered With Notes"); `spec-quality.mjs --strict` 0 lỗi |
| 2026-08-11 | `US-016` | ingest | 7 (feature `Draft`, feature-summary, pbi rỗng, source-record, business rule mới `BR-019`, `ENT-002-danh-muc` cập nhật, `EPC-002` mục 4 cập nhật) — không tạo epic mới, gắn vào `EPC-002` (F2) đã có sẵn; requirement là defect + opportunity phát hiện qua PO review (`docs/po/review-2026-08-11-loai-chi-tieu-combobox.md`, PO-03), gọi bởi `ssr-ba` trong `ssr-pipeline US-016`; `openItemsCount=0` (cả 3 câu hỏi mở của raw đã tự trả lời/được user xác nhận trước khi vào ingest) |
| 2026-08-11 | `US-016` | sync | 6 (feature → Active, pbi → 8 AC, feature-summary, source-record, `EPC-002` mục 4 → Active) — spec đạt `Ready for DEV` ngay lượt đầu, không cần dialog thêm (0 câu hỏi `Cần user xác nhận` — mọi điểm mờ đã chốt trước qua `DEC-073` và xác nhận trực tiếp của user); `po-expert` xác nhận `Aligned` (đúng M1, F2, xử lý đúng phần `DEC-056` bị ảnh hưởng); `ba-expert` sửa 4 điểm không đổi ý định nghiệp vụ (thêm AC-08 cho ngoại lệ lỗi lưu vốn đã mô tả ở mục 6 nhưng thiếu AC, bổ sung `BR-010` vào mục 2, mở rộng mục 11 với AC-01/AC-03 của US-005 bị ảnh hưởng, sửa số đếm AC ở mục 12); `spec-quality.mjs --strict` 0 lỗi |
| 2026-08-12 | `US-017` | ingest | 8 (feature `Draft`, feature-summary, pbi rỗng, source-record, business rule mới `BR-020`, `BR-016` thêm liên kết, `ENT-002-danh-muc` cập nhật, `EPC-002` mục 4 cập nhật) — không tạo epic/entity mới, gắn vào `EPC-002` (F2) đã có sẵn; requirement là tính năng mới do user yêu cầu trực tiếp qua `/dylan-ssrkit:ssr-po` → chuyển `ssr-raw`; `openItemsCount=1` (`BR-020` mục 4: thứ tự danh mục khi Clone tháng — giả định hợp lý của `ssr-raw`, chưa hỏi trực tiếp user, cần `ssr-ba` gom vào dialog) |
| 2026-08-12 | `US-017` | sync | 7 (feature → Active, pbi → 8 AC, feature-summary, source-record, `BR-020` bổ sung hành vi Clone tháng + xóa nhãn `Cần user xác nhận`, `BR-016` thêm ghi chú, `EPC-002` mục 4 → Active) — spec đạt `Ready for DEV` sau 1 lượt dialog chốt hành vi Clone tháng (`DEC-078`); `po-expert` lần 1 `Needs Adjustment` (US-017 không nằm trong khoảng trống mục 7 Business Flow, trong khi US-007/US-009/US-011 cùng tier "Trung bình" đã có sẵn DEC nhưng chưa có spec) → user chọn tiếp tục US-017 trước qua dialog (`DEC-079`, theo tiền lệ `DEC-067`) → `po-expert` gọi lại xác nhận `Aligned`; `ba-expert` sửa cross-link sai ở `EL-02`/`EL-03` mục 8.1 và bổ sung `AC-08` cho ngoại lệ lỗi lưu đã mô tả ở mục 6 nhưng thiếu AC; `spec-quality.mjs --strict` 0 lỗi |
| 2026-08-13 | `US-018` | ingest | 8 (feature `Draft`, feature-summary, pbi rỗng, source-record, entity mới `ENT-004-job-ung-tuyen`, entity mới `ENT-005-platform-tuyen-dung`, business rule mới `BR-021`) — không gắn epic (Business Flow hiện tại chỉ phạm vi Hệ Thống Quản Lý Chi Tiêu F1-F4, không bao gồm trang Roadmap — mục 1 Business Flow liệt kê "roadmap" là mục tách biệt ở M2); requirement do user yêu cầu trực tiếp, chuyển qua `ssr-po` → `ssr-raw`; `openItemsCount=1` (trạng thái mặc định khi thêm job mới — giả định hợp lý "Interested", chưa hỏi trực tiếp user; ngoài ra còn 3 điểm "cần ssr-ba làm rõ" ghi trong raw mục 5: input control Ngày hết hạn, validate Link, luồng chuyển trạng thái có tuần tự hay không) |
| 2026-08-13 | `US-018` | sync | 5 (feature → Active, pbi → 11 AC, feature-summary, source-record, mục 9 feature ghi rõ lý do không gắn epic) — spec đạt `Ready for DEV` sau 2 lượt dialog trong `ssr-ba` (`DEC-084`..`DEC-087` chốt 4 điểm mờ của raw; `DEC-088`, `DEC-089` chốt sau khi `ba-expert`/`po-expert` chạy song song lần 1: `po-expert` lần 1 `Blocked` vì US-018 nằm ngoài phạm vi Business Flow chưa có quyết định tường minh, `ba-expert` phát hiện thiếu luồng sửa 5 trường ngoài Trạng thái); user chốt qua dialog "US-018 là tiện ích cá nhân tách biệt, không cần mở rộng Business Flow" (`DEC-088`) và "sửa job theo cách inline" (`DEC-089`) → gọi lại riêng `po-expert`, xác nhận `Aligned`; `spec-quality.mjs --strict` 0 lỗi (1 cảnh báo Small — 11 AC vượt ngưỡng khuyến nghị 8, không bắt buộc tách) |
| 2026-08-14 | `US-019` | ingest | 9 (feature `Draft`, feature-summary, pbi rỗng, source-record, entity mới `ENT-006-item-can-mua`, business rule mới `BR-022`, `BR-023`, `BR-024`, `EPC-003` mục 4 cập nhật — đề xuất) — không tạo epic mới, đề xuất gắn `EPC-003` (F3) vì việc chuyển item Pending gắn liền với hành động tạo tháng mới (dùng chung `BR-015`), nhưng Business Flow hiện tại chưa liệt kê US-019 ở mục 6/7 nên cần `po-expert` xác nhận khi `ssr-ba` chạy (theo tiền lệ `US-018`/`DEC-088`); requirement do user yêu cầu trực tiếp, qua `ssr-raw` (6 câu hỏi mở, chốt `DEC-092`..`DEC-097`); `openItemsCount=1` (`BR-023` mục 4: việc chuyển item áp dụng cho cả 2 cách tạo tháng hay chỉ "Clone tháng đang xem" — giả định không đủ cơ sở để tự trả lời, cần `ssr-ba` gom vào dialog) |
| 2026-08-14 | `US-020` | ingest | 8 (feature `Draft`, feature-summary, pbi rỗng, source-record, business rule mới `BR-025`, `BR-026`, `BR-027`, `ENT-004-job-ung-tuyen` cập nhật — thêm mốc "Ngày nộp hồ sơ" và trạng thái "Expired") — không tạo epic mới, không gắn epic (cùng lý do/tiền lệ `US-018`/`DEC-088` — mở rộng trực tiếp entity và trang Roadmap của `US-018`, ngoài phạm vi Business Flow "Hệ Thống Quản Lý Chi Tiêu"); requirement do user yêu cầu trực tiếp qua `ssr-pipeline` → `ssr-raw` (4 câu hỏi mở chốt qua dialog: `DEC-099`..`DEC-102`); `openItemsCount=0` (raw không còn dòng nào `Cần user xác nhận` — 2 câu còn lại của raw là `Giả định hợp lý`, không chặn) |
| 2026-08-14 | `US-020` | sync | 5 (feature → Active, pbi → 9 AC, feature-summary, source-record, mục 9 feature ghi rõ `po-expert` xác nhận `Aligned`) — spec đạt `Ready for DEV` sau 1 lượt dialog trong `ssr-ba` (`DEC-103`, `DEC-104` chốt 2 điểm mờ phát sinh khi viết spec, ngoài 4 điểm đã chốt sẵn ở `ssr-raw`); `ba-expert` bổ sung `AC-09` (ngoại lệ job Waiting chưa từng có mốc "Ngày nộp hồ sơ" — lỗ hổng tiêu chí chấp nhận trước đó) và sửa 2 điểm số liệu (đếm spec đã rà, đếm DEC theo đúng ssr-raw/ssr-ba); `po-expert` xác nhận `Aligned` ngay lượt đầu (áp dụng hợp lý tiền lệ `DEC-088` vì US-020 mở rộng cùng entity/cùng trang, không mở phạm vi mới); `spec-quality.mjs --strict` 0 lỗi (1 cảnh báo advisory Small — 9 AC vượt ngưỡng khuyến nghị 8, không bắt buộc tách) |
| 2026-08-14 | `US-019` | sync | 9 (feature → Active + gắn epic chính thức, pbi → 10 AC, feature-summary, source-record, `BR-023` xóa nhãn `Cần user xác nhận`, `BR-024` bổ sung định nghĩa "sửa" = inline, `EPC-003` mục 4 → Active, indexes) — spec đạt `Ready for DEV` sau nhiều lượt: (1) dialog trong `ssr-ba` chốt `BR-023` (cả hai nút "Tạo tháng"/"Clone tháng đang xem" đều mang item Pending — `DEC-098`); (2) `po-expert` lần 1 `Blocked` vì US-019 ngoài phạm vi Business Flow → user chọn "mở rộng Business Flow" (khác tiền lệ `DEC-088` của US-018) → chuyển `ssr-po mode=business-flow` thêm mục tiêu mới `M3`, gắn `US-019` vào F3 (`DEC-105`); (3) `ba-expert` phát hiện mâu thuẫn nội bộ (các trang kia dùng chữ "sửa" nhưng spec chưa có luồng sửa) → dialog bổ sung khả năng sửa tên/giá inline (`DEC-106`), AC tăng từ 8 lên 10; `po-expert` gọi lại lần 2 xác nhận `Aligned`; `spec-quality.mjs --strict` 0 lỗi (1 cảnh báo advisory Small — 10 AC vượt ngưỡng khuyến nghị 8, không bắt buộc tách) |
| 2026-08-21 | `US-007` | ingest | 6 (feature `Draft`, feature-summary, pbi rỗng, source-record, epic mới `EPC-004` cho F4 — lần đầu F4 có epic trong cấu trúc nested, business rule mới `BR-028`) — migrate từ trang phẳng cũ, gọi bởi `ssr-ba` trong `ssr-pipeline US-007`; `openItemsCount=1` (Q1 gốc từ raw: có cần giới hạn số tháng khi tính "toàn bộ lịch sử" vì lý do hiệu năng không — cần `ssr-ba` gom vào dialog) |
| 2026-08-21 | `US-007` | sync | 6 (feature → Active + thu hẹp đúng phạm vi, pbi → 4 AC, feature-summary, source-record, `BR-028` thu hẹp nội dung, `EPC-004` → Active) — spec đạt `Ready for DEV` sau 2 lượt dialog: (1) chốt không giới hạn số tháng (`DEC-109`); (2) `ba-expert` phát hiện `BR-028`/wiki feature bản ingest ban đầu mô tả phạm vi rộng hơn spec (gồm cả thẻ insight và biểu đồ "Cơ cấu chi tiêu") → dialog xác nhận thu hẹp chỉ đúng biểu đồ "Xu hướng" (`DEC-110`); `ba-expert` cũng bổ sung `AC-04` (ngoại lệ chưa có dữ liệu) và làm rõ Given/Then của `AC-03`; `po-expert` xác nhận `Aligned` ngay lượt đầu (đúng M1, F4, không mâu thuẫn DEC nào Active); `spec-quality.mjs --strict` 0 lỗi |
| 2026-08-21 | `US-008` | ingest | 4 (feature `Draft`, feature-summary, pbi rỗng, source-record, business rule mới `BR-029`) — không tạo epic mới, gắn vào `EPC-004` (F4) đã có sẵn từ `US-007`; migrate từ trang phẳng cũ, gọi bởi `ssr-ba` trong `ssr-pipeline US-008`; `openItemsCount=0` (raw không còn câu hỏi mở nào chặn spec) |
| 2026-08-21 | `US-008` | sync | 5 (feature → Active, pbi → 4 AC, feature-summary, source-record, `EPC-004` mục 4 → Active) — spec đạt `Ready for DEV` ngay lượt đầu, không cần dialog (0 câu hỏi `Cần user xác nhận` — raw đã tự trả lời đủ); `po-expert` xác nhận `Aligned` ngay lượt đầu (đúng M1, F4, không mâu thuẫn DEC nào Active); `ba-expert` chỉ bổ sung AC-03 làm rõ item cần mua ở tháng khác cũng phải xuất đầy đủ, không phát sinh đề xuất cần user quyết; `spec-quality.mjs --strict` 0 lỗi |
| 2026-08-22 | `US-009` | ingest | 4 (feature `Draft`, feature-summary, pbi rỗng, source-record, business rule mới `BR-030`) — không tạo epic mới, gắn vào `EPC-002` (F2) đã có sẵn; migrate từ trang phẳng cũ, gọi bởi `ssr-ba` trong `ssr-pipeline US-009`; `openItemsCount=1` (quy tắc kế thừa ngưỡng khi Clone tháng từ tháng nguồn không phải tháng gần nhất — cần `ssr-ba` gom vào dialog) |

## 5. Nợ Kỹ Thuật (Ghi Chú Ngoài Template Chuẩn)

2 US khác (US-001, US-011) vẫn ở dạng trang phẳng `docs/kb/ba/wiki/US-###-*.md`, chưa migrate sang cấu trúc nested này. `.ssr-kit.env` của dự án thiếu các biến `SSR_BA_WIKI_INGESTION`/`SSR_BA_WIKI_KNOWLEDGE`/`SSR_BA_WIKI_DELIVERY`/`SSR_BA_WIKI_GOVERNANCE`/`SSR_BACKLOG` mà bản kit 2.6.0 kỳ vọng. Không chặn triển khai (US-001 đã Delivered qua quy trình cũ trước khi hook `SSR-E010` được siết chặt), nhưng nên được user quyết định: (a) cập nhật `.ssr-kit.env` và migrate toàn bộ, hoặc (b) giữ nguyên trạng và chỉ dùng cấu trúc nested cho US mới từ nay. Xem `docs/memory/judgement-log.md#jdg-005`.
